import React from 'react';
import {View, Text} from 'react-native'

const Poll = (props) => {
    return (
        <View>
            <Text>
                Poll
            </Text>
        </View>
    );
};

export default Poll;
